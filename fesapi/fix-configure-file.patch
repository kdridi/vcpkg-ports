--- fesapi-2.14.0.0/CMakeLists.txt	2025-10-28 10:10:02
+++ fesapi-2.14.0.0-modified/CMakeLists.txt	2026-01-23 10:14:28
@@ -83,6 +83,8 @@
 	MESSAGE(ERROR " The ZLIB_INCLUDE_DIR CMake variable is mandatory.")
 ENDIF ()
 FIND_PACKAGE (MINIZIP REQUIRED)
+# Find the actual unofficial-minizip target
+find_package(unofficial-minizip CONFIG REQUIRED)
 
 # Szip (only useful when using static linking to HDF5)
 set (SZIP_LIBRARY_RELEASE SZIP_LIBRARY_RELEASE-NOTFOUND CACHE FILEPATH "Path to the file which contains the szip library release")
--- fesapi-2.14.0.0/src/CMakeLists.txt	2025-10-28 10:10:02
+++ fesapi-2.14.0.0-modified/src/CMakeLists.txt	2026-01-23 10:14:18
@@ -9,7 +9,9 @@
 add_library (${CPP_LIBRARY_NAME} ${LIB_TYPE} "")
 
 # Create the version config header file in order for fesapi users to know the fesapi version at runtime.
-configure_file(${FESAPI_ROOT_DIR}/cmake/version_config.h.in ${FESAPI_ROOT_DIR}/src/version_config.h)
+if(NOT EXISTS ${FESAPI_ROOT_DIR}/src/version_config.h)
+	configure_file(${FESAPI_ROOT_DIR}/cmake/version_config.h.in ${FESAPI_ROOT_DIR}/src/version_config.h)
+endif()
 
 #configure the HDF5 hidt type to use in Fesapi according to the HDF5 version
 if (HDF5_VERSION VERSION_LESS "1.10")
@@ -26,7 +28,9 @@
 		target_compile_definitions(${CPP_LIBRARY_NAME} PRIVATE "HDF5_ROS3_ENABLED")
 	endif(HDF5_ROS3_ENABLED)
 endif ()
-configure_file(${FESAPI_ROOT_DIR}/cmake/HidtType.h.in ${FESAPI_ROOT_DIR}/src/common/HidtType.h)
+if(NOT EXISTS ${FESAPI_ROOT_DIR}/src/common/HidtType.h)
+	configure_file(${FESAPI_ROOT_DIR}/cmake/HidtType.h.in ${FESAPI_ROOT_DIR}/src/common/HidtType.h)
+endif()
 
 # Define the name of the directories where the various Energistics namespaces sources are located.
 set (RESQML_PREFIX_2 "resqml2")
@@ -62,14 +66,14 @@
 	target_link_libraries (${CPP_LIBRARY_NAME} PRIVATE ${HDF5_LIBRARIES})
 else()
 	# Do not mix with hdf5::hdf5 (lowercase) which does not export HDF5 DEFINITIONS every time on windows
-	target_link_libraries (${CPP_LIBRARY_NAME} PRIVATE HDF5::HDF5)
+	target_link_libraries (${CPP_LIBRARY_NAME} PRIVATE hdf5::hdf5-static)
 endif()
 
 if (TARGET ZLIB::ZLIB)
-	target_link_libraries (${CPP_LIBRARY_NAME} PRIVATE ZLIB::ZLIB MINIZIP::MINIZIP)
+	target_link_libraries (${CPP_LIBRARY_NAME} PRIVATE ZLIB::ZLIB unofficial::minizip::minizip)
 else ()
 	target_include_directories(${CPP_LIBRARY_NAME} SYSTEM PRIVATE ${ZLIB_INCLUDE_DIR})
-	target_link_libraries (${CPP_LIBRARY_NAME} PRIVATE MINIZIP::MINIZIP)
+	target_link_libraries (${CPP_LIBRARY_NAME} PRIVATE unofficial::minizip::minizip)
 endif ()
 
 target_link_libraries (${CPP_LIBRARY_NAME} PRIVATE Boost::boost)
@@ -138,7 +142,9 @@
 set (FESAPI_EML2_0_NS eml2_0 CACHE STRING "Namespace for eml2_0 objects")
 set (FESAPI_EML2_3_NS eml2_3 CACHE STRING "Namespace for eml2_3 objects")
 
-configure_file(${FESAPI_ROOT_DIR}/cmake/nsDefinitions.h ${FESAPI_ROOT_DIR}/src/nsDefinitions.h) # Overwrite if different
+if(NOT EXISTS ${FESAPI_ROOT_DIR}/src/nsDefinitions.h)
+	configure_file(${FESAPI_ROOT_DIR}/cmake/nsDefinitions.h ${FESAPI_ROOT_DIR}/src/nsDefinitions.h)
+endif() # Overwrite if different
 
 include(${CMAKE_CURRENT_SOURCE_DIR}/common/CMakeLists.txt)
 include(${CMAKE_CURRENT_SOURCE_DIR}/tools/CMakeLists.txt)
--- fesapi-2.14.0.0/cmake/FesapiCppConfig.cmake.in	2025-10-28 10:10:02
+++ fesapi-2.14.0.0-modified/cmake/FesapiCppConfig.cmake.in	2026-01-23 10:41:58
@@ -1,4 +1,10 @@
 include(CMakeFindDependencyMacro)
 
+# Find dependencies
+find_dependency(HDF5 REQUIRED)
+find_dependency(unofficial-minizip CONFIG REQUIRED)
+find_dependency(ZLIB REQUIRED)
+find_dependency(boost CONFIG REQUIRED)
+
 # Include the actual targets
 include("${CMAKE_CURRENT_LIST_DIR}/FesapiCppTargets.cmake")
